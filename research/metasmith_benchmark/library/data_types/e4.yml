types:
  # metaGEM's own tool versions, for the E4 GEM lane only. They live here, not in bench or
  # modelling, because rebuilding either of those re-mints transform ids other experiments cache on.

  carveme_122.env:
    properties:
      e4: env
      provides: [carveme]
  memote_0913.env:
    properties:
      e4: env
      provides: [memote]
