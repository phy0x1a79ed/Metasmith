# DeepVirFinder at the jessieren/DeepVirFinder clone baked into this image (Theano and Keras on CPU,
# models at /DeepVirFinder/models). Bioconda carries no DeepVirFinder package.
container: docker://multifractal/deepvirfinder:0.1
