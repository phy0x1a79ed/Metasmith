# Run working directories. Drivers default their --output here (`nostoc_ecspr.py`,
# `nostoc_ecspr_measure.py`), and what is worth keeping is copied into a DVC chunk and
# pinned. Nothing here is an artifact.
/scratch/
/kbase
/lineage-report-demo
