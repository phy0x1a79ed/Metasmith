ontology:
  doi: https://doi.org/10.1093/bioinformatics/btt113
  name: EDAM
  strict: false
  version: '1.25'
schema: v1
types:
  aam_cache:
    properties:
    - 'The mapper members'' durable cache, one directory per member holding `cache.tsv`
      and the attempted-ids sidecars. STAGED EMPTY ON A FIRST RUN and that is the
      ordinary state: it has no producer, because the thing that writes it is the
      same lane that reads it. It exists because the in-task cache lives in node-local
      scratch and is discarded on retry, so a lane OOM-killed two thirds of the way
      through lost all of it. A cached row is reused only when the SUBMISSION STRING
      it was produced from is the one this run would send -- a reaction has one id
      and up to four strings, and serving the wrong one is not staleness but a map
      of a different molecule filed under this one''s name.'
  chebi:
    properties:
    - 'ChEBI flat files (chebi/rel<N>/): compounds, names and structures as served,
      gzipped. Taken from `archive/rel<N>/flat_files/`, NOT from the rolling `flat_files/`
      path -- the archive is frozen at its own URL, so the release number is a true
      pin rather than a label on whatever is current. Feeds the curation sweep''s
      supplier lane: a name and an accession are the only handles a structureless
      MetaNetX stub has.'
  equilibrator:
    properties:
    - 'eQuilibrator compound cache (equilibrator/<package-version>/): compounds.sqlite
      + cc_params.npz, with the Zenodo checksums they are verified against. Pinned
      explicitly so a build is not silently dependent on the package''s first-use
      fetch.'
  metacyc:
    properties:
    - 'The MetaCyc flat-file distribution as dropped in (metacyc/<version>/). Licensed
      and not redistributable: it is the reason this graph has a given at all. Verified
      here, never fetched.'
  metanetx:
    properties:
    - 'MetaNetX/MNXref release tree (metanetx/<version>/): chem_prop, chem_xref, reac_prop,
      reac_xref and their upstream .md5 sidecars. The identity space everything else
      is expressed in.'
  modelseed:
    properties:
    - The ModelSEED biochemistry compound table (modelseed/<commit>/), pinned by the
      git commit that last touched it. ModelSEED publishes no releases and the file
      is served from a branch, so the commit sha IS the version -- and it is a stronger
      pin than a release number, because it names the exact tree.
  prior_bake_logs:
    properties:
    - 'A previous metabolism bake''s per-reaction records -- `indigo_status.tsv`,
      `indigo_unreturned.tsv`, `attempted/`, and the two derived neural status tables.
      THE EMPIRICAL HALF OF `aam_forecast`: which reaction Indigo hangs on is not
      a function of the string and does not need to be, because the last run wrote
      it down. Coverage may be PARTIAL -- a run that dies leaves nothing about the
      reactions past where it died -- so a reaction absent from every table was never
      asked, and the forecast reports that denominator rather than reading absence
      as success.'
